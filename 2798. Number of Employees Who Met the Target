class Solution {
    public int numberOfEmployeesWhoMetTarget(int[] hours, int target) {
        int x=hours.length;
        int count=0;
        for(int i=0;i<x;i++)
        {
            if(hours[i]>=target)
            {
                count++;
            }
        }
        return count;
    }
}

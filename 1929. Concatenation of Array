class Solution {
    public int[] getConcatenation(int[] nums) {
        int z=nums.length;
        int ans[]=new int[2*z];
        
        for (int i=0;i<z;i++)
        {

            ans[i]=nums[i];
            ans[i+z]=nums[i];
        }
            
        return ans;
      
    }     
   
}
